/**
 * #include <bits/stdc++.h>
 * // This is a bad practise, because the libraries that are included in the bits folder, area compiler specific.
 * This library contains all the typical libraries that have to use in the context of competitive programming.
 */
#include <iostream>
#include <string>

using namespace std;

// We can compile this program using the following command:
// g++ -std=c++11 -O2 -Wall template.cpp -o template
int main() {

    return 0;
}